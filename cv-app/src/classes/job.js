class Job {
  constructor(place, title, description, startDate, endDate, id) {
    this.place = place;
    this.title = title;
    this.description = description;
    this.startDate = startDate;
    this.endDate = endDate;
    this.id = id;
  }
}

export default Job;
