class Job {
  constructor(place, title, description, startDate, endDate) {
    this.place = place;
    this.title = title;
    this.description = description;
    this.startDate = startDate;
    this.endDate = endDate;
  }
}

export default Job;
