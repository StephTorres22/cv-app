class Establishment {
  constructor(
    name, startDate, endDate) {
    // this.id = id;
    this.name = name;
    this.startDate = startDate;
    this.endDate = endDate;
  }
}

export default Establishment;
